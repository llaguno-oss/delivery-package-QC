# Core UI framework — install one of the following:
#   pip install PySide6        (Qt 6, recommended)
#   pip install PySide2        (Qt 5, legacy)
PySide6>=6.0

# Optional: codec and resolution checking via ffprobe
# (no pip install needed - ffprobe is part of ffmpeg)
# Install ffmpeg from https://ffmpeg.org/download.html
# or: brew install ffmpeg / apt install ffmpeg / winget install Gyan.FFmpeg
